# ADINN Planning Task Manager Backend

This backend uses MongoDB Atlas. Add `MONGODB_URI` before running.

```bash
cp .env.example .env
npm install
npm run dev
```

Health check:

```text
/api/health
```
