# ADINN Planning Task Manager Backend

This backend uses PostgreSQL. Add `DATABASE_URL` before running.

```bash
cp .env.example .env
npm install
npm run dev
```

Health check:

```text
/api/health
```
